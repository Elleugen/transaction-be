spring.application.name=transactions

# PostgreSQL configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/transactions_db
spring.datasource.username=transactions_user
spring.datasource.password=supgsql234qwe

# Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Spring Security default user (for simplicity)
spring.security.user.name=admin
spring.security.user.password=admin
